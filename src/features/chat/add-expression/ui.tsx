import { Expressions } from '@shared/icons';
import { IconButton } from '@shared/ui';

export const AddExpression = (): JSX.Element => {
  return (
    <IconButton>
      <Expressions />
    </IconButton>
  );
};
